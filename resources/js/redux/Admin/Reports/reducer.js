import actions from './actions';

const initialState = {
  branchesData: [],
  trashedData: [],
  expenseSectionsData: [],
  paymentsData: []
}

function Reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}

export default Reducer;
